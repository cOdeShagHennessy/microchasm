{
  "name": "twiglet",
  "version": "0.0.1",
  "scripts": {
    "test": "lab -v --colors --leaks ",
    "test-coverage": "lab -r html -o coverage.html"
  },
  "dependencies": {
    "tracer": "^0.7.4",
    "colors": "^1.1.0"
  },
  "devDependencies": {
    "code": "^1.3.0",
    "lab": "^5.5.0"
  }
}
